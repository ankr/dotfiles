[alias]
	st = status
	co = checkout
	nb = checkout -b
	ap = add -p
	df = diff
	ci = commit
	cv = commit -v
	br = branch
	ca = commit --amend
	cp = cherry-pick
	lg = log -p
	fakeignore = update-index --assume-unchanged
	find = !git log --color -p -S
	graph = log --all --graph --decorate --oneline -n40
	undo = reset HEAD^1
[color]
	branch  = auto
	diff = auto
	status = auto
[core]
	excludesfile = ~/.gitignore
[include]
	path = ~/.dotfiles/git_custom
[push]
	default = current
